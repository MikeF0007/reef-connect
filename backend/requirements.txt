# Database & ORM - Core dependencies for SQLAlchemy + Alembic setup
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
asyncpg==0.30.0
aiosqlite==0.20.0  # For SQLite async support

# Configuration & Settings
python-dotenv==1.0.1
pydantic==2.10.6
pydantic-settings==2.7.1

# Authentication & Security (for auth utilities)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography>=42.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0

# Dev tools
ruff==0.9.9

# NOTE: Service-specific dependencies (FastAPI, etc.) are in each service's requirements.txt
# when the API structure is set up
